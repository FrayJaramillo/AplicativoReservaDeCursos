{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "docker-compose-build",
			"command": "docker-compose",
			"args": [
				"-f",
				"${workspaceFolder}/docker-compose.yml",
				"build"
			],
			"presentation": {
				"reveal": "silent"
			}
		}
	]
}