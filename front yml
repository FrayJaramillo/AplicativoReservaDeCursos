  # Servicio de Frontend (React)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend_react
    restart: unless-stopped
    ports:
      - "3000:80"
    networks:
      - university_network



      api de registro de clases, esta api se requiere que liste las clases 

      modificar la aplicacion de reservas ya que tambien ser reserva un docnete